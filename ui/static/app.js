// Minimal client-side JS. The pages are mostly form-based + server-rendered;
// this file is here for any progressive-enhancement bits.

// Auto-close tracking modal popup window if it was opened with target="tracking"
// (harmless if not).
window.addEventListener("load", () => {
  // No-op for now; placeholder.
});
